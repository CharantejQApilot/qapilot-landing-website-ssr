/**
 * SEOHead is a no-op in the Next.js SSR build.
 * SEO metadata is handled via Next.js metadata exports in each page.tsx.
 * This stub exists to prevent import errors from components that still reference it.
 */
export default function SEOHead(_props: Record<string, unknown>) {
  return null;
}
